// List all the gcloud project

gcloud projects list

// set config

gcloud config set project foxbooking

// gcloud compute instances list
 
// ssh to a cloud instance

gcloud compute ssh cks-master
